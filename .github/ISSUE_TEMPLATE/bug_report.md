---
name: Bug report
about: Something is broken (build, plugin, runtime, counterexamples, replays)
labels: bug
---

**What happened / what did you expect?**

**Minimal reproduction** (a `@BmcProof` + the code under proof, or build config):

```java
```

**Environment**: bmc4j version, OS/arch, JDK, Kotlin (if relevant)

**Output** (the failure message / stack trace / `--info` excerpt):

```
```
