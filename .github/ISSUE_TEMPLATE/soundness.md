---
name: Soundness report
about: "The serious one: bmc4j reported VERIFIED for code that is refutable within the stated bounds"
labels: bug, soundness
---

<!--
A false green is bmc4j's equivalent of a security vulnerability. If the impact
is sensitive, consider GitHub private vulnerability reporting instead (see
SECURITY.md). Documented limits (docs/limits.md, docs/coverage.md) are not
soundness bugs - the boundary is SILENT unsoundness within claimed coverage.
-->

**The proof that shows green:**

```java
```

**The concrete input that violates the property** (and how you found it):

```java
```

**Why this is inside claimed coverage** (which docs/coverage.md row, which bound):

**Environment**: bmc4j version, OS/arch, JDK/Kotlin
