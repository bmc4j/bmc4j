# No version here: the release TAG is the version's source of truth, surfaced to
# the build as the BMC4J_VERSION env var; everything else builds as 0.0.1-local
# (see the version block in build.gradle.kts).
